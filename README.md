# Sales Dashboard (Next.js 15 + TypeScript + TailwindCSS)

## Features
- Atomic design principle (atoms, molecules, organisms).
- Sales chart using Recharts.
- Custom filter input for threshold-based filtering.
- Dashboard page with integrated components.

## Setup
```bash
git clone <your-repo-url>
cd project-folder
npm install
npm run dev
